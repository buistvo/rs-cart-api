service: cart-api

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs16.x
  region: 'eu-north-1'

functions:
  main:
    handler: dist/main.handler
    events:
      - http:
          method: ANY
          path: /
      - http:
          method: ANY
          path: '{proxy+}'
